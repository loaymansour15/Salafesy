# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Salafesy' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Salafesy
	pod 'Alamofire', '~> 4.7'
	pod 'Firebase/Core'
	pod 'Firebase/Database'
	pod 'Firebase/Auth'

  target 'SalafesyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SalafesyUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
